# This is used common between all portfolios

symbols_meta:
  QQQ:
    group: Index
    can_trade: false
    contract_type: Equity
    min_required_move_from_level: 0.25
    breakout_confirmation_distance: 0.15
    chart_entry_offset: 0.5
    retest_tolerance_multiplier: 1
    exchange: CBOE
    expiry_offset: 1
  TSLA:
    group: G1
    can_trade: true
    contract_type: Equity
    min_required_move_from_level: 0.25
    breakout_confirmation_distance: 0.15
    chart_entry_offset: 1
    retest_tolerance_multiplier: 1.8
  AAPL:
    can_trade: true
    contract_type: Equity
    min_required_move_from_level: 0.25
    breakout_confirmation_distance: 0.15
    chart_entry_offset: 0.5
    retest_tolerance_multiplier: 1
  NVDA:
    can_trade: true
    contract_type: Equity
    min_required_move_from_level: 0.25
    breakout_confirmation_distance: 0.15
    chart_entry_offset: 0.5
    retest_tolerance_multiplier: 1
  AMD:
    can_trade: true
    contract_type: Equity
    min_required_move_from_level: 0.25
    breakout_confirmation_distance: 0.15
    chart_entry_offset: 0.5
    retest_tolerance_multiplier: 1
  PLTR:
    can_trade: true
    contract_type: Equity
    min_required_move_from_level: 0.25
    breakout_confirmation_distance: 0.15
    chart_entry_offset: 0.5
    retest_tolerance_multiplier: 1
  TSLL:
    group: G1
    can_trade: true
    contract_type: Equity
    min_required_move_from_level: 0.04
    breakout_confirmation_distance: 0.07
    chart_entry_offset: 0.5
    retest_tolerance_multiplier: 1.8
  SPY:
    group: Index
    can_trade: false
    contract_type: Equity
    min_required_move_from_level: 0.25
    breakout_confirmation_distance: 0.15
    chart_entry_offset: 0.5
    retest_tolerance_multiplier: 2
    exchange: CBOE
    expiry_offset: 1
  MNQ:
    can_trade: false
    contract_type: Future
    min_required_move_from_level: 5
    breakout_confirmation_distance: 2
    chart_entry_offset: 20
    retest_tolerance_multiplier: 3
    contract_month: 202603
    long:
      stop_loss: level_used - ( dynamic_tolerance.get('tolerance', 0) * app_config['symbols_meta'][symbol].get('retest_tolerance_multiplier', 1))
      take_profit: df['close'].iloc[-1] + (df['atr_14'].iloc[-2] * 2 )
    short:
      stop_loss: level_used + ( dynamic_tolerance.get('tolerance', 0) * app_config['symbols_meta'][symbol].get('retest_tolerance_multiplier', 1))
      take_profit: df['close'].iloc[-1] - (df['atr_14'].iloc[-2] * 2 )
  AMZN:
    can_trade: true
    contract_type: Equity
    min_required_move_from_level: 0.25
    breakout_confirmation_distance: 0.15
    chart_entry_offset: 0.5
    retest_tolerance_multiplier: 1


cases:
  case_1:
    memo: trade if the levels are close to each other - we not runing this one anymore
    precondition: all_levels_in(symbol)  # , 'back_test'
    can_replace_level: false
    long:
      level: max(levels['PDH'], levels['PMH'], levels['5MH'])
      cores:
        - breakout_in_last_x_candles(side='up', idx_list=[-5], level=long_level)
        - price_retest(side='up', idx_list=[-2,-3,-4], level=long_level, both_sides=True )
        - is_retest_after_breakout(side='up', level=long_level)
        - check_entry_vs_retest(side='up', level=long_level)
        - no_failure_after_breakout(side='up', ohlc_field='close',level=long_level)
        - check_price_vs_level(side='up', price=price, level=long_level )
        - are_levels_close_to_each_other_for_case_1(side='up')
        - 930 < last_record_hh_mm < 1016 or  mode == 'live'

    short:
      level: min(levels['PDL'], levels['PML'], levels['5ML'])
      cores:
        - breakout_in_last_x_candles(side='down', idx_list=[-5], level=short_level)
        - price_retest(side='down', idx_list=[-2,-3,-4], level=short_level, both_sides=True )
        - is_retest_after_breakout(side='down', level=short_level)
        - check_entry_vs_retest(side='down', level=short_level)
        - no_failure_after_breakout(side='down', ohlc_field='close',level=short_level)
        - check_price_vs_level(side='down', price=price, level=short_level )
        - are_levels_close_to_each_other_for_case_1(side='down')
        - 930 < last_record_hh_mm < 1016 or  mode == 'live'

  case_2:
    memo: This is trading on 5MH and 5ML - release candidate - we not running any more.
    can_replace_level: true
    precondition: all_levels_in(symbol)
    long:
      level: levels['5MH']
      cores:
        - breakout_in_last_x_candles(side='up', idx_list=[-3,-4,-5,-6,-7,-8,-9,-10,-11], level=long_level)
        - price_retest(side='up', idx_list=[-2,-3,-4], level=long_level, both_sides=True )
        - is_retest_after_breakout(side='up', level=long_level)
        - check_entry_vs_retest(side='up', level=long_level)
        - no_failure_after_breakout(side='up', ohlc_field='close',level=long_level)
        - check_price_vs_level(side='up', price=price, level=long_level )
        - not is_price_close_to_next_levels_ver_2(side='up', price=price, current_level=long_level, next_levels=['PDH'])
        - 930 < last_record_hh_mm < 1016
    short:
      level: levels['5ML']
      cores:
      - breakout_in_last_x_candles(side='down', idx_list=[-3,-4,-5,-6,-7,-8,-9,-10,-11], level=short_level)
      - price_retest(side='down', idx_list=[-2,-3,-4], level=short_level, both_sides=True )
      - is_retest_after_breakout(side='down', level=short_level)
      - check_entry_vs_retest(side='down', level=short_level)
      - no_failure_after_breakout(side='down', ohlc_field='close',level=short_level)
      - check_price_vs_level(side='down', price=price, level=short_level )
      - not is_price_close_to_next_levels_ver_2(side='down', price=price, current_level=short_level, next_levels=['PDL'])
      - 930 < last_record_hh_mm < 1016

  case_2_1:
    memo: 5MH and 5ML breakout_ver_2, no short above PDH, trade close to level. - we running this one
    can_replace_level: true
    precondition: all_levels_in(symbol)
    long:
      level: levels['5MH']
      cores:
        - breakout_in_last_x_candles_ver_2(side='up', idx_list=[-3,-4,-5,-6,-7,-8,-9,-10,-11], level=long_level)
        - price_retest(side='up', idx_list=[-2,-3,-4], level=long_level, both_sides=True )
        - is_retest_after_breakout(side='up', level=long_level)
        - check_entry_vs_retest(side='up', level=long_level)
        - no_failure_after_breakout(side='up', ohlc_field='close',level=long_level)
        - check_price_vs_level(side='up', price=price, level=long_level )
        - not is_price_close_to_next_levels_ver_2(side='up', price=price, current_level=long_level, next_levels=['PDH'])
        - 930 < last_record_hh_mm < 1016 or mode == 'live'
        - abs (price - long_level) < 1.2 * atr_14
        - price > application_state['symbols'][symbol]['PDL'] and application_state['symbols']['QQQ']['price'] > application_state['symbols']['QQQ']['PDL']
        - price < levels['PDH'] # avoid going long above PDH - extra safety- if we are above PDH, low change we continue after 5MH breakout
    short:
      level: levels['5ML']
      cores:
        - breakout_in_last_x_candles_ver_2(side='down', idx_list=[-3,-4,-5,-6,-7,-8,-9,-10,-11], level=short_level)
        - price_retest(side='down', idx_list=[-2,-3,-4], level=short_level, both_sides=True )
        - is_retest_after_breakout(side='down', level=short_level)
        - check_entry_vs_retest(side='down', level=short_level)
        - no_failure_after_breakout(side='down', ohlc_field='close',level=short_level)
        - check_price_vs_level(side='down', price=price, level=short_level )
        - not is_price_close_to_next_levels_ver_2(side='down', price=price, current_level=short_level, next_levels=['PDL'])
        - 930 < last_record_hh_mm < 1016 or  mode == 'live'
        - abs (price - short_level) < 1.2 * atr_14 or 1 == 1
        - price < application_state['symbols'][symbol]['PDH'] and application_state['symbols']['QQQ']['price'] < application_state['symbols']['QQQ']['PDH']
        - price > levels['PDL'] # avoid going short below PDL - extra safety- if we are below PDL, low change we continue after 5ML breakout


  case_2_1_async:
    memo: 5MH and 5ML breakout_ver_2, no short above PDH, trade close to level. - we running this one
    can_replace_level: true
    precondition: all_levels_in(application_state, symbol)
    long:
      level: levels['5MH']
      level_alias: '5MH'
      cores:
        - breakout_in_last_x_candles_ver_2(app_config, application_state, symbol, df, side='up', idx_list=[-3,-4,-5,-6,-7,-8,-9,-10,-11], level=long_level, level_alias=level_alias)
        - price_retest(app_config, application_state, symbol, df, side='up', idx_list=[-2,-3,-4], level=long_level, both_sides=True, level_alias=level_alias)
        - is_retest_after_breakout(application_state, symbol, side='up', level=long_level)
        - check_entry_vs_retest(application_state, case, symbol, df, side='up', level=long_level)
        - no_failure_after_breakout(application_state, case, symbol, df, side='up', level=long_level, ohlc_field='close')
        - check_price_vs_level(app_config, symbol, side='up', price=price, level=long_level )
        - not is_price_close_to_next_levels_ver_2(app_config, application_state, symbol, df, side='up', price=price, current_level=long_level, next_levels=['PDH'])
        - 930 < get_last_record_hh_mm(df) < 1016 or get_mode(application_state) == 'live'
      extras:
        - abs (price - long_level) < 1.2 * atr_14
        - price > application_state['levels'][symbol]['PDL'] and get_current_price(ib, application_state, 'QQQ') > application_state['levels']['QQQ']['PDL']
        - price < levels['PDH'] # avoid going long above PDH - extra safety- if we are above PDH, low change we continue after 5MH breakout
    short:
      level: levels['5ML']
      level_alias: '5ML'
      cores:
        - breakout_in_last_x_candles_ver_2(app_config, application_state, symbol, df, side='down', idx_list=[-3,-4,-5,-6,-7,-8,-9,-10,-11], level=short_level, level_alias=level_alias)
        - price_retest(app_config, application_state, symbol, df, side='down', idx_list=[-2,-3,-4], level=short_level, both_sides=True ,level_alias=level_alias)
        - is_retest_after_breakout(application_state, symbol, side='down', level=short_level)
        - check_entry_vs_retest(application_state, case, symbol, df, side='down', level=short_level)
        - no_failure_after_breakout(application_state, case, symbol, df, side='down', level=short_level, ohlc_field='close')
        - check_price_vs_level(app_config, symbol, side='down', price=price, level=short_level )
        - not is_price_close_to_next_levels_ver_2(app_config, application_state, symbol, df, side='down', price=price, current_level=short_level, next_levels=['PDH'])
        - 930 < get_last_record_hh_mm(df) < 1016 or get_mode(application_state) == 'live'
      extras:
        - abs (price - short_level) < 1.2 * atr_14
        - price < application_state['levels'][symbol]['PDH'] and get_current_price(ib, application_state, 'QQQ') < application_state['levels']['QQQ']['PDH']
        - price > levels['PDL'] # avoid going long above PDH - extra safety- if we are above PDH, low change we continue after 5MH breakout

  case_2_2:
    memo: 5MH and 5ML. using breakout_ver_2. GAPs incorporated  - we decided to HOLD this
    can_replace_level: true
    precondition: all_levels_in(symbol)
    long:
      level: levels['5MH']
      cores:
        - breakout_in_last_x_candles_ver_2(side='up', idx_list=[-3,-4,-5,-6,-7,-8,-9,-10,-11], level=long_level)
        - price_retest(side='up', idx_list=[-2,-3,-4], level=long_level, both_sides=True )
        - is_retest_after_breakout(side='up', level=long_level)
        - check_entry_vs_retest(side='up', level=long_level)
        - no_failure_after_breakout(side='up', ohlc_field='close',level=long_level)
        - check_price_vs_level(side='up', price=price, level=long_level )
        - not is_price_close_to_next_levels_ver_2(side='up', price=price, current_level=long_level, next_levels=['PDH'])
        - QQQ_gap_up_in_current_candle()
        - 930 < last_record_hh_mm < 1016 or mode == 'live'
    short:
      level: levels['5ML']
      cores:
        - breakout_in_last_x_candles_ver_2(side='down', idx_list=[-3,-4,-5,-6,-7,-8,-9,-10,-11], level=short_level)
        - price_retest(side='down', idx_list=[-2,-3,-4], level=short_level, both_sides=True )
        - is_retest_after_breakout(side='down', level=short_level)
        - check_entry_vs_retest(side='down', level=short_level)
        - no_failure_after_breakout(side='down', ohlc_field='close',level=short_level)
        - check_price_vs_level(side='down', price=price, level=short_level )
        - not is_price_close_to_next_levels_ver_2(side='down', price=price, current_level=short_level, next_levels=['PDL'])
        - QQQ_gap_down_in_current_candle()
        - 930 < last_record_hh_mm < 1016 or  mode == 'live'
  case_4:
    memo: From PDH in first few mins
    can_replace_level: false
    precondition: 1 == 1  # all_levels_in(symbol)
    long:
      level: levels['PDH']
      cores:
        - breakout_in_last_x_candles_ver_2(side='up', idx_list=[-3,-4,-5], level=long_level)
        - price_retest(side='up', idx_list=[-2,-3,-4], level=long_level, both_sides=True )
        - is_retest_after_breakout(side='up', level=long_level)
        - check_entry_vs_retest(side='up', level=long_level)
        - no_failure_after_breakout(side='up', ohlc_field='close',level=long_level)
        - check_price_vs_level(side='up', price=price, level=long_level )
        - abs (price - long_level) < 1.2 * atr_14 or  1 == 1
        - 930 < last_record_hh_mm < 940
    short:
      level: levels['PDL']
      cores:
        - breakout_in_last_x_candles_ver_2(side='down', idx_list=[-3,-4,-5], level=short_level)
        - price_retest(side='down', idx_list=[-2,-3,-4], level=short_level, both_sides=True )
        - is_retest_after_breakout(side='down', level=short_level)
        - check_entry_vs_retest(side='down', level=short_level)
        - no_failure_after_breakout(side='down', ohlc_field='close',level=short_level)
        - check_price_vs_level(side='down', price=price, level=short_level )
        - abs (price - short_level) < 1.2 * atr_14 or 1 == 1
        - 930 < last_record_hh_mm < 940
  case_4_1_async:
    memo: From PDH PDL - we running this one
    can_replace_level: false
    precondition: all_levels_in(application_state, symbol)
    long:
      level: levels['PDH']
      level_alias: 'PDH'
      cores:
        - breakout_in_last_x_candles_ver_2(app_config, application_state, symbol, df, side='up', idx_list=[-3,-4,-5,-6,-7,-8,-9,-10,-11], level=long_level)
        - price_retest(app_config, application_state, symbol, df, side='up', idx_list=[-2,-3,-4], level=long_level, both_sides=True, level_alias=level_alias)
        - is_retest_after_breakout(application_state, symbol, side='up', level=long_level)
        - check_entry_vs_retest(application_state, case, symbol, df, side='up', level=long_level)
        - no_failure_after_breakout(application_state, case, symbol, df, side='up', ohlc_field='close',level=long_level)
        - check_price_vs_level(app_config, symbol, side='up', price=price, level=long_level )
      extras:
        - abs (price - long_level) < 1.2 * atr_14
        - levels['PDH'] < levels['5MH']
        - 930 < get_last_record_hh_mm(df) < 1016 or get_mode(application_state) == 'live'
        - not is_price_close_to_next_levels_ver_2(app_config, application_state, symbol, df, side='up', price=price, current_level=long_level, next_levels=['PMH'])

    short:
      level: levels['PDL']
      level_alias: 'PDL'
      memo: conditon 8 need to be checeked
      cores:
        - breakout_in_last_x_candles_ver_2(app_config, application_state, symbol, df, side='down', idx_list=[-3,-4,-5,-6,-7,-8,-9,-10,-11], level=short_level)
        - price_retest(app_config, application_state, symbol, df, side='down', idx_list=[-2,-3,-4], level=short_level, both_sides=True, level_alias=level_alias)
        - is_retest_after_breakout(application_state, symbol, side='down', level=short_level)
        - check_entry_vs_retest(application_state, case, symbol, df, side='down', level=short_level)
        - no_failure_after_breakout(application_state, case, symbol, df, side='down', ohlc_field='close',level=short_level)
        - check_price_vs_level(app_config, symbol, side='down', price=price, level=short_level )
      extras:
        - abs (price - long_level) < 1.2 * atr_14
        - levels['PDL'] > levels['5ML']
        - 930 < get_last_record_hh_mm(df) < 1016 or  get_mode(application_state) == 'live'
        - not is_price_close_to_next_levels_ver_2(app_config, application_state, symbol, df, side='down', price=price, current_level=short_level, next_levels=['PML'])

  case_3:
    memo: This is for Saeed's live test
    can_replace_level: false
    precondition: all_levels_in(application_state, symbol)
    long:
      level: levels['5MH']
      level_alias: '5MH'
      cores:
        - symbol == 'TSLA' and 1 == 2

    short:
      level: levels['5ML']
      level_alias: '5ML'
      cores:
        - symbol == 'AMD' and 1 == 2
      extras:
        - 1 == 2
