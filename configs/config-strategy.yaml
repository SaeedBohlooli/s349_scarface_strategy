
cases:
  case_2:
    memo: 5MH and 5ML breakout_ver_2, no short above PDH, trade close to level. - we running this one - 2.1 improved
    can_replace_level: true
    precondition: all_levels_in(application_state, symbol)
    color: blue
    long:
      level: levels['5MH']
      level_alias: "5MH"
      cores:
        - breakout_in_last_x_candles_ver_4(app_config, application_state, case, symbol, df, side='up', idx_list=[-3,-4,-5,-6,-7,-8,-9,-10,-11], level=long_level, level_alias=level_alias)
        - price_retest(app_config, application_state, case, symbol, df, side='up', idx_list=[-2,-3,-4], level=long_level, both_sides=True, level_alias=level_alias)
        - set_and_is_retest_after_breakout(application_state, symbol, case, side='up', level=long_level)
        - check_entry_vs_retest(application_state, case, symbol, df, side='up', level=long_level)
        # 5
        - no_failure_after_breakout(application_state, case, symbol, df, side='up', level=long_level, ohlc_field='close')
        - check_price_vs_level(app_config, symbol, side='up', price=price, level=long_level )
        - 930 < get_last_record_hh_mm(df) < 1016 or get_mode(application_state) == 'live'
      extras:
        - skip_level_closeness_enabled or not is_price_close_to_next_levels_ver_2(app_config, application_state, case, symbol, df, side='up', price=price, current_level=long_level, next_levels=['PDH'])
        - abs (price - long_level) < 1.2 * atr_14
        # 10
        - 10 == 10 and price > application_state['levels'][symbol]['PDL']
        - skip_pdhl_and_qqq_check_enabled or price < levels['PDH'] or get_current_price(ib, application_state, 'QQQ') > application_state['levels']['QQQ']['5MH'] # avoid going long above PDH - extra safety- if we are above PDH, low change we continue after 5MH breakout.
        - abs (price - long_level) < 2
        - qqq_df['close'].iloc[-1] > qqq_df['open'].iloc[-1]  # forming candle.

        # TODO get the range of 5MH and 5ML and check the current price is close to 5MHL and 5ML

    short:
      level: levels['5ML']
      level_alias: "5ML"
      cores:
        - breakout_in_last_x_candles_ver_4(app_config, application_state, case, symbol, df, side='down', idx_list=[-3,-4,-5,-6,-7,-8,-9,-10,-11], level=short_level, level_alias=level_alias)
        - price_retest(app_config, application_state, case, symbol, df, side='down', idx_list=[-2,-3,-4], level=short_level, both_sides=True ,level_alias=level_alias)
        - set_and_is_retest_after_breakout(application_state, symbol, case, side='down', level=short_level)
        - check_entry_vs_retest(application_state, case, symbol, df, side='down', level=short_level)
        # 5
        - no_failure_after_breakout(application_state, case, symbol, df, side='down', level=short_level, ohlc_field='close')
        - check_price_vs_level(app_config, symbol, side='down', price=price, level=short_level )
        - 930 < get_last_record_hh_mm(df) < 1016 or get_mode(application_state) == 'live'
      extras:
        - not is_price_close_to_next_levels_ver_2(app_config, application_state, case, symbol, df, side='down', price=price, current_level=short_level, next_levels=['PDL'])
        - abs (price - short_level) < 1.2 * atr_14
        # 10
        - 10 == 10 and price < application_state['levels'][symbol]['PDH'] # avoid going long above PDH - extra safety- if we are above PDH, low change we continue after 5MH breakout
        - price > levels['PDL'] or get_current_price(ib, application_state, 'QQQ') < application_state['levels']['QQQ']['5ML']
        - abs (price - short_level) < 2
        - qqq_df['close'].iloc[-1] < qqq_df['open'].iloc[-1]

  case_4:
    memo: From PDH PDL - we running this one
    can_replace_level: false
    precondition: all_levels_in(application_state, symbol)
    color: green
    long:
      level: levels['PDH']
      level_alias: "PDH"
      cores:
        - breakout_in_last_x_candles_ver_4(app_config, application_state, case, symbol, df, side='up', idx_list=[-3,-4,-5,-6,-7,-8,-9,-10,-11], level=long_level, level_alias=level_alias)
        - price_retest(app_config, application_state, case, symbol, df, side='up', idx_list=[-2,-3,-4], level=long_level, both_sides=True, level_alias=level_alias)
        - set_and_is_retest_after_breakout(application_state, symbol, case, side='up', level=long_level)
        - check_entry_vs_retest(application_state, case, symbol, df, side='up', level=long_level)
        # 5
        - no_failure_after_breakout(application_state, case, symbol, df, side='up', ohlc_field='close',level=long_level)
        - check_price_vs_level(app_config, symbol, side='up', price=price, level=long_level )
        - 930 < get_last_record_hh_mm(df) < 1016 or get_mode(application_state) == 'live'
      extras:
        - levels['PDH'] < levels['5MH'] or get_current_price(ib, application_state, 'QQQ') > application_state['levels']['QQQ']['5MH']
        - abs (price - long_level) < 1.2 * atr_14
        # 10
        - 10 == 10 and ( skip_level_closeness_enabled or ( not is_price_close_to_next_levels_ver_2(app_config, application_state, case, symbol, df, side='up', price=price, current_level=long_level, next_levels=['5MH'])))
        - abs (price - long_level) < 2
        - qqq_df['close'].iloc[-1] >= qqq_df['open'].iloc[-1]

    short:
      level: levels['PDL']
      level_alias: "PDL"
      memo: conditon 8 need to be checeked
      cores:
        - breakout_in_last_x_candles_ver_4(app_config, application_state, case, symbol, df, side='down', idx_list=[-3,-4,-5,-6,-7,-8,-9,-10,-11], level=short_level, level_alias=level_alias)
        - price_retest(app_config, application_state, case, symbol, df, side='down', idx_list=[-2,-3,-4], level=short_level, both_sides=True, level_alias=level_alias)
        - set_and_is_retest_after_breakout(application_state, symbol, case, side='down', level=short_level)
        - check_entry_vs_retest(application_state, case, symbol, df, side='down', level=short_level)
        # 5
        - no_failure_after_breakout(application_state, case, symbol, df, side='down', ohlc_field='close',level=short_level)
        - check_price_vs_level(app_config, symbol, side='down', price=price, level=short_level )
        - 930 < get_last_record_hh_mm(df) < 1016 or get_mode(application_state) == 'live'
      extras:
        - levels['PDL'] > levels['5ML'] or get_current_price(ib, application_state, 'QQQ') < application_state['levels']['QQQ']['5ML']
        - abs (price - short_level) < 1.2 * atr_14
        # 10
        - 10 == 10 and not is_price_close_to_next_levels_ver_2(app_config, application_state, case, symbol, df, side='down', price=price, current_level=short_level, next_levels=['PML'])
        - not is_price_close_to_next_levels_ver_2(app_config, application_state, case, symbol, df, side='down', price=price, current_level=short_level, next_levels=['5ML'])
        - abs (price - short_level) < 2
        - qqq_df['close'].iloc[-1] <= qqq_df['open'].iloc[-1]


  case_7:
    memo: based on the R2
    can_replace_level: false
    precondition: all_levels_in(application_state, symbol)
    color: orange
    long:
      level: levels['5MH']
      level_alias: "5MH"
      cores:
        - breakout_in_last_x_candles_ver_4(app_config, application_state, case, symbol, df, side='up', idx_list=list(range(-3,-17,-1)), level=long_level, level_alias=level_alias)
        - price_retest_v2(app_config, application_state, case, symbol, df, side='up', idx_list=[-2,-3], level=long_level, level_alias=level_alias)
        - set_and_is_retest_after_breakout(application_state, symbol, case, side='up', level=long_level)
        - does_arc_has_enough_heights(app_config, application_state, case, symbol, df, side='up', level=long_level, level_alias=level_alias, min_height_atr_ratio=0.5)
        - check_arc_duration(app_config, application_state, case, symbol, df, side='up', level=long_level, level_alias=level_alias, arc_min_bars=4)
        - check_close_displacement(app_config, application_state, case, symbol, df, side='up', level=long_level, level_alias=level_alias, min_close_disp_atr=0.1)
        - price > long_level and df.iloc[-1]['open'] > long_level and df.iloc[-1]['close'] > df.iloc[-1]['open']
      extras:
        - is_second_candle_after(min_seconds=15)
        - 9 == 9
        - has_strong_close(df, side='up')
        - is_symbol_in_rs_shortlist(app_config, application_state, symbol, side='up', threshold=5)


    short:
      level: levels['5ML']
      level_alias: "5ML"
      cores:
        - breakout_in_last_x_candles_ver_4(app_config, application_state, case, symbol, df, side='down', idx_list=list(range(-3,-17,-1)), level=short_level, level_alias=level_alias)
        - price_retest_v2(app_config, application_state, case, symbol, df, side='down', idx_list=[-2,-3], level=short_level, level_alias=level_alias)
        - set_and_is_retest_after_breakout(application_state, symbol, case, side='down', level=short_level)
        - does_arc_has_enough_heights(app_config, application_state, case, symbol, df, side='down', level=short_level, level_alias=level_alias, min_height_atr_ratio=0.5)
        - check_arc_duration(app_config, application_state, case, symbol, df, side='down', level=short_level, level_alias=level_alias, arc_min_bars=4)
        - check_close_displacement(app_config, application_state, case, symbol, df, side='down', level=short_level, level_alias=level_alias, min_close_disp_atr=0.1)
        - price < short_level and df.iloc[-1]['open'] < short_level and df.iloc[-1]['close'] < df.iloc[-1]['open']
      extras:
        - is_second_candle_after(min_seconds=15)
        - 9 == 9
        - has_strong_close(df, side='down')
        - is_symbol_in_rs_shortlist(app_config, application_state, symbol, side='down', threshold=5)

  case_8:
    memo: based on the R2
    can_replace_level: false
    precondition: all_levels_in(application_state, symbol)
    color: deeppink
    long:
      level: levels['PDH']
      level_alias: "PDH"
      cores:
        - breakout_in_last_x_candles_ver_4(app_config, application_state, case, symbol, df, side='up', idx_list=list(range(-3,-17,-1)), level=long_level, level_alias=level_alias)
        - price_retest_v2(app_config, application_state, case, symbol, df, side='up', idx_list=[-2,-3], level=long_level, level_alias=level_alias)
        - set_and_is_retest_after_breakout(application_state, symbol, case, side='up', level=long_level)
        - does_arc_has_enough_heights(app_config, application_state, case, symbol, df, side='up', level=long_level, level_alias=level_alias, min_height_atr_ratio=0.5)
        - check_arc_duration(app_config, application_state, case, symbol, df, side='up', level=long_level, level_alias=level_alias, arc_min_bars=4)
        - check_close_displacement(app_config, application_state, case, symbol, df, side='up', level=long_level, level_alias=level_alias, min_close_disp_atr=0.1)
        - price > long_level and df.iloc[-1]['open'] > long_level and df.iloc[-1]['close'] > df.iloc[-1]['open']
      extras:
        - is_second_candle_after(min_seconds=15)
        - 9 == 9
        - has_strong_close(df, side='up')
        - is_symbol_in_rs_shortlist(app_config, application_state, symbol, side='up', threshold=5)

    short:
      level: levels['PDL']
      level_alias: "PDL"
      cores:
        - breakout_in_last_x_candles_ver_4(app_config, application_state, case, symbol, df, side='down', idx_list=list(range(-3,-17,-1)), level=short_level, level_alias=level_alias)
        - price_retest_v2(app_config, application_state, case, symbol, df, side='down', idx_list=[-2,-3], level=short_level, level_alias=level_alias)
        - set_and_is_retest_after_breakout(application_state, symbol, case, side='down', level=short_level)
        - does_arc_has_enough_heights(app_config, application_state, case, symbol, df, side='down', level=short_level, level_alias=level_alias, min_height_atr_ratio=0.5)
        - check_arc_duration(app_config, application_state, case, symbol, df, side='down', level=short_level, level_alias=level_alias, arc_min_bars=4)
        - check_close_displacement(app_config, application_state, case, symbol, df, side='down', level=short_level, level_alias=level_alias, min_close_disp_atr=0.1)
        - price < short_level and df.iloc[-1]['open'] < short_level and df.iloc[-1]['close'] < df.iloc[-1]['open']
      extras:
        - is_second_candle_after(min_seconds=15)
        - 9 == 9
        - has_strong_close(df, side='down')
        - is_symbol_in_rs_shortlist(app_config, application_state, symbol, side='down', threshold=5)

  case_9:
    memo: 5MH and 5ML and rs
    can_replace_level: true
    precondition: all_levels_in(application_state, symbol)
    color: blue
    long:
      level: levels['5MH']
      level_alias: "5MH"
      cores:
        - breakout_in_last_x_candles_ver_4(app_config, application_state, case, symbol, df, side='up', idx_list=[-3,-4,-5,-6,-7,-8,-9,-10,-11], level=long_level, level_alias=level_alias)
        - price_retest(app_config, application_state, case, symbol, df, side='up', idx_list=[-2,-3,-4], level=long_level, both_sides=True, level_alias=level_alias)
        - set_and_is_retest_after_breakout(application_state, symbol, case, side='up', level=long_level)
        - check_entry_vs_retest(application_state, case, symbol, df, side='up', level=long_level)
        # 5
        - no_failure_after_breakout(application_state, case, symbol, df, side='up', level=long_level, ohlc_field='close')
        - check_price_vs_level(app_config, symbol, side='up', price=price, level=long_level )
        - 930 < get_last_record_hh_mm(df) < 1016 or get_mode(application_state) == 'live'
      extras:
        - skip_level_closeness_enabled or not is_price_close_to_next_levels_ver_2(app_config, application_state, case, symbol, df, side='up', price=price, current_level=long_level, next_levels=['PDH'])
        - abs (price - long_level) < 1.2 * atr_14
        # 10
        - 10 == 10 and price > application_state['levels'][symbol]['PDL']
        - skip_pdhl_and_qqq_check_enabled or price < levels['PDH'] or get_current_price(ib, application_state, 'QQQ') > application_state['levels']['QQQ']['5MH'] # avoid going long above PDH - extra safety- if we are above PDH, low change we continue after 5MH breakout.
        - abs (price - long_level) < 2
        - qqq_df['close'].iloc[-1] > qqq_df['open'].iloc[-1]  # forming candle.
        - is_symbol_in_rs_shortlist(app_config, application_state, symbol, side='up', threshold=5)
        - 15 == 15

    short:
      level: levels['5ML']
      level_alias: "5ML"
      cores:
        - breakout_in_last_x_candles_ver_4(app_config, application_state, case, symbol, df, side='down', idx_list=[-3,-4,-5,-6,-7,-8,-9,-10,-11], level=short_level, level_alias=level_alias)
        - price_retest(app_config, application_state, case, symbol, df, side='down', idx_list=[-2,-3,-4], level=short_level, both_sides=True ,level_alias=level_alias)
        - set_and_is_retest_after_breakout(application_state, symbol, case, side='down', level=short_level)
        - check_entry_vs_retest(application_state, case, symbol, df, side='down', level=short_level)
        # 5
        - no_failure_after_breakout(application_state, case, symbol, df, side='down', level=short_level, ohlc_field='close')
        - check_price_vs_level(app_config, symbol, side='down', price=price, level=short_level )
        - 930 < get_last_record_hh_mm(df) < 1016 or get_mode(application_state) == 'live'
      extras:
        - not is_price_close_to_next_levels_ver_2(app_config, application_state, case, symbol, df, side='down', price=price, current_level=short_level, next_levels=['PDL'])
        - abs (price - short_level) < 1.2 * atr_14
        # 10
        - 10 == 10 and price < application_state['levels'][symbol]['PDH'] # avoid going long above PDH - extra safety- if we are above PDH, low change we continue after 5MH breakout
        - skip_pdhl_and_qqq_check_enabled or price > levels['PDL'] or get_current_price(ib, application_state, 'QQQ') < application_state['levels']['QQQ']['5ML']
        - abs (price - short_level) < 2
        - qqq_df['close'].iloc[-1] < qqq_df['open'].iloc[-1]
        - is_symbol_in_rs_shortlist(app_config, application_state, symbol, side='down', threshold=5)
        - 15 == 15

  case_10:
    memo: From PDH PDL - we running this one
    can_replace_level: false
    precondition: all_levels_in(application_state, symbol)
    color: green
    long:
      level: levels['PDH']
      level_alias: "PDH"
      cores:
        - breakout_in_last_x_candles_ver_4(app_config, application_state, case, symbol, df, side='up', idx_list=[-3,-4,-5,-6,-7,-8,-9,-10,-11], level=long_level, level_alias=level_alias)
        - price_retest(app_config, application_state, case, symbol, df, side='up', idx_list=[-2,-3,-4], level=long_level, both_sides=True, level_alias=level_alias)
        - set_and_is_retest_after_breakout(application_state, symbol, case, side='up', level=long_level)
        - check_entry_vs_retest(application_state, case, symbol, df, side='up', level=long_level)
        # 5
        - no_failure_after_breakout(application_state, case, symbol, df, side='up', ohlc_field='close',level=long_level)
        - check_price_vs_level(app_config, symbol, side='up', price=price, level=long_level )
        - 930 < get_last_record_hh_mm(df) < 1016 or get_mode(application_state) == 'live'
      extras:
        - levels['PDH'] < levels['5MH'] or get_current_price(ib, application_state, 'QQQ') > application_state['levels']['QQQ']['5MH']
        - abs (price - long_level) < 1.2 * atr_14
        # 10
        - 10 == 10 and ( skip_level_closeness_enabled or ( not is_price_close_to_next_levels_ver_2(app_config, application_state, case, symbol, df, side='up', price=price, current_level=long_level, next_levels=['5MH'])))
        - abs (price - long_level) < 2
        - qqq_df['close'].iloc[-1] >= qqq_df['open'].iloc[-1]
        - is_symbol_in_rs_shortlist(app_config, application_state, symbol, side='up', threshold=5)

    short:
      level: levels['PDL']
      level_alias: "PDL"
      memo: conditon 8 need to be checeked
      cores:
        - breakout_in_last_x_candles_ver_4(app_config, application_state, case, symbol, df, side='down', idx_list=[-3,-4,-5,-6,-7,-8,-9,-10,-11], level=short_level, level_alias=level_alias)
        - price_retest(app_config, application_state, case, symbol, df, side='down', idx_list=[-2,-3,-4], level=short_level, both_sides=True, level_alias=level_alias)
        - set_and_is_retest_after_breakout(application_state, symbol, case, side='down', level=short_level)
        - check_entry_vs_retest(application_state, case, symbol, df, side='down', level=short_level)
        # 5
        - no_failure_after_breakout(application_state, case, symbol, df, side='down', ohlc_field='close',level=short_level)
        - check_price_vs_level(app_config, symbol, side='down', price=price, level=short_level )
        - 930 < get_last_record_hh_mm(df) < 1016 or get_mode(application_state) == 'live'
      extras:
        - levels['PDL'] > levels['5ML'] or get_current_price(ib, application_state, 'QQQ') < application_state['levels']['QQQ']['5ML']
        - abs (price - short_level) < 1.2 * atr_14
        # 10
        - 10 == 10 and not is_price_close_to_next_levels_ver_2(app_config, application_state, case, symbol, df, side='down', price=price, current_level=short_level, next_levels=['PML'])
        - not is_price_close_to_next_levels_ver_2(app_config, application_state, case, symbol, df, side='down', price=price, current_level=short_level, next_levels=['5ML'])
        - abs (price - short_level) < 2
        - qqq_df['close'].iloc[-1] <= qqq_df['open'].iloc[-1]
        - is_symbol_in_rs_shortlist(app_config, application_state, symbol, side='down', threshold=5)

  case_3:
    memo: This is for Saeed's live test
    can_replace_level: false
    precondition: all_levels_in(application_state, symbol)
    color: darkred
    long:
      level: levels['5MH']
      level_alias: "5MH"
      cores:
        - symbol == 'AMD' and 1 == 2
      extras:
        - 1 == 1
    short:
      level: levels['PDL']
      level_alias: "5ML"
      cores:
        - symbol == 'TSLA' and 1 == 2
      extras:
        - 1 == 1


stop_losses:
    - right == 'C' and case != 'case_manual' and underlying_previous_candle_close < level_used_to_open - tolerance_amount and underlying_current_price < underlying_previous_candle_close
    - right == 'P' and case != 'case_manual' and underlying_previous_candle_close > level_used_to_open + tolerance_amount and underlying_current_price > underlying_previous_candle_close
    - right == 'C' and user_defined_stop_loss !=0 and underlying_current_price < user_defined_stop_loss
    - right == 'P' and user_defined_stop_loss !=0 and underlying_current_price > user_defined_stop_loss
    - right == 'C' and case in ('case_7', 'case_8') and underlying_previous_candle_close < level_used_to_open - tolerance_amount
    - right == 'P' and case in ('case_7', 'case_8') and underlying_previous_candle_close > level_used_to_open + tolerance_amount
#    - current_bid < entry_price * 0.70

take_profits:
  t1:
    enabled: true
    condition: >
      (current_bid > entry_price * percentage_change or current_bid - entry_price > absolute_change)
      and entry_price not in [0,-1] and current_bid not in [0,-1]
    close_quantity_percentage: 0.6
    close_quantity_numbner: total - number_of_trails
    memo: Taking some profit at 15% or 0.20 with 4 of 6 - percentage_change comes from config

  t2:
    enabled: true
    condition: >
      (current_bid > entry_price * 1.30 or current_bid - entry_price > 0.70)
      and entry_price not in [0,-1] and current_bid not in [0,-1]
    close_quantity_percentage: 0.20
    memo: Taking some profit at 30% or 0.80 with 1 of 6

  t3:
    enabled: true
    condition: >
      (current_bid > entry_price * 1.60 )
      and entry_price not in [0,-1] and current_bid not in [0,-1]
    close_quantity_percentage: -1

  t4_ev_if_t1:
    enabled: true
    condition: >
      (right == 'C' and is_executed_take_profits(application_state, symbol, take_profit_list=['t1']) and underlying_previous_candle_close < entry_underlying_price)
      or
      (right == 'P' and is_executed_take_profits(application_state, symbol, take_profit_list=['t1']) and underlying_previous_candle_close > entry_underlying_price)
    close_quantity_percentage: -1
    memo: This is for break even - if we hit TP1, then we go for break even

  t4_ev_if_t2:
    enabled: true
    condition: >
      (right == 'C' and is_executed_take_profits(application_state, symbol, take_profit_list=['t2']) and underlying_previous_candle_close < entry_underlying_price)
      or
      (right == 'P' and is_executed_take_profits(application_state, symbol, take_profit_list=['t2']) and underlying_previous_candle_close > entry_underlying_price)
    close_quantity_percentage: -1

    memo: This is for break even - if we hit TP2, then we go for break even

  t5_cr_lv:
    enabled: true
    condition: >
      (right == 'C' and is_price_crossed_levels(application_state, side='up', symbol=symbol, next_levels=['PDH'], current_price=underlying_current_price, entry_underlying_price=entry_underlying_price))
      or
      (right == 'P' and is_price_crossed_levels(application_state, side='down', symbol=symbol, next_levels=['PDL'], current_price=underlying_current_price, entry_underlying_price=entry_underlying_price))
    close_quantity_percentage: 0.5
    memo: if we entered below major level and crossing level, we dump some ...

  t6_trail_t1:
    enabled: true
    condition: check_mark_revers_candles(application_state, symbol,take_profit_alias='t2', market_data=market_data)
    close_quantity_percentage: -1
    memo: for long after tp1, if we go below last down close candle, then we close the position and vice versa for short

  t6_trail_w_ema_9:
    enabled: false  # TODO check EMA_9 before anabling it
    condition: symbol_df['close'].iloc[-1] < symbol_df['ema_9'].iloc[-1]  # for long after tp1, if we go below ema 9, then we close the position and vice versa for short
    close_quantity_percentage: -1
    memo: for long after tp1, if we go below last down close candle, then we close the position and vice versa for short

  t7_user_defined_take_profit:
    enabled: true
    always_run: true
    multiple_run: true
    close_type: "real_absolute"
    condition: >
      (right == 'C' and underlying_current_price > user_defined_take_profit and user_defined_take_profit not in [0,-1])
      or
      (right == 'P' and underlying_current_price < user_defined_take_profit and user_defined_take_profit not in [0,-1])
    close_quantity_numbner: user_defined_take_profit_quantity
    memo:

  t10_cl_manual: # This is for closing everything just NVDA:   1 == 1 and symbol == 'NVDA'
    enabled: False
    condition: 1 == 2 and symbol == 'AMZN'
    close_quantity_percentage: .5

  t11_cl_manual: # This is for closing everything just NVDA:   1 == 1 and symbol == 'NVDA'
    enabled: False
    condition: 1 == 3 and symbol == 'TSLA'
    close_quantity_percentage: -1


closeness_distance: df['atr_14'].iloc[-2]

closeness_distance_for_chart:
  live: df['atr_14'].tail(10).mean()
  back_test: df.loc[ (df['date'].dt.time == pd.to_datetime('10:00').time()), 'atr_14' ].mean() # for BT, we need to get for 10:00 as for BT we don't go candle base...

closeness_distance_for_case_1: df['atr_14'].iloc[-2] * 2

indicators:
  enabled: False
  calculation_interval_seconds: 60
  print_last_few_rows: True
  details:
      one:
        active: true
        outputs_in_application_state:
        - EMA_9
        calculation: |
          df['EMA_9'] = df['close'].ewm(span=9, adjust=False).mean()
#      two:
#        active: true
#        outputs_in_application_state:
#        - EMA_20
#        calculation: |
#          df['EMA_20'] = df['close'].ewm(span=20, adjust=False).mean()
#      three:
#        active: true
#        outputs_in_application_state:
#        - EMA_50
#        calculation: |
#          df['EMA_50'] = df['close'].ewm(span=50, adjust=False).mean()
#      four:
#        active: true
#        outputs_in_application_state:
#        - VWAP
#        memo: Session Volume Weighted Average Price
#        calculation: |
#          df['_tp']     = (df['high'] + df['low'] + df['close']) / 3
#          df['_tp_vol'] = df['_tp'] * df['volume']
#          df['_date']   = df.date
#          df['VWAP']    = (df.groupby('_date')['_tp_vol'].transform('cumsum') /
#                           df.groupby('_date')['volume'].transform('cumsum'))
#          df.drop(columns=['_tp', '_tp_vol', '_date'], inplace=True)
#      five:
#        active: true
#        outputs_in_application_state:
#        - EMA_9_slope_pct
#        - EMA_20_slope_pct
#        - EMA_50_slope_pct
#        - EMA_9_direction
#        - EMA_20_direction
#        - EMA_50_direction
#
#        memo: slope_period controls how many bars back to compare — 3 bars on 1m = ~3 min lag
#        calculation: |
#          slope_period = 3
#          threshold    = 0.02          # % — below this = flat
#          for col in ['EMA_9', 'EMA_20', 'EMA_50']:
#              slope = (df[col] - df[col].shift(slope_period)) / df[col].shift(slope_period) * 100
#              df[f'{col}_slope_pct'] = slope
#              df[f'{col}_direction'] = np.where(slope >  threshold, 'up',
#                                      np.where(slope < -threshold, 'down', 'flat'))
